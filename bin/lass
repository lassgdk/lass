#!/usr/bin/env python

import sys, os
from PySide import QtGui
from lass.pmtools import DIR_LASS_DATA
from lass.gui.ui.general import MainWindow

if __name__ == '__main__':

    app = QtGui.QApplication(sys.argv)
    # with open(os.path.join(DIR_LASS_DATA, "gui", "main.qss")) as styleSheetFile:
        # app.setStyleSheet(styleSheetFile.read())
    window = MainWindow()
    window.reloadStyle()
    window.show()
    ret = app.exec_()
    sys.exit( ret )
